# 📦 Создание установочного файла SharkDraw

## Вариант 1: Один .exe файл (Рекомендуется)

### Шаг 1: Установите PyInstaller
```powershell
python -m pip install pyinstaller
```

### Шаг 2: Создайте .exe файл
```powershell
python build_installer.py
```

Или вручную:
```powershell
pyinstaller --name=SharkDraw --onefile --windowed --icon=assets/Logo.ico --add-data="assets;assets" --add-data="src;src" main.py
```

### Шаг 3: Найдите готовый файл
После сборки файл будет здесь:
```
dist/SharkDraw.exe
```

### Шаг 4: Отправьте другому пользователю
Просто скопируйте `SharkDraw.exe` и отправьте. Пользователь запускает двойным кликом!

---

## Вариант 2: Архив с папкой (Быстрее)

Если PyInstaller не работает, просто заархивируйте всю папку:

### Шаг 1: Создайте ZIP архив
```powershell
Compress-Archive -Path "C:\Users\poker\OneDrive\Рабочий стол\PaintPro" -DestinationPath "SharkDraw.zip"
```

### Шаг 2: Отправьте архив
Пользователь должен:
1. Распаковать архив
2. Установить Python 3.14
3. Установить зависимости: `pip install -r requirements.txt`
4. Запустить через `SharkDraw.vbs`

---

## Вариант 3: Installer с Inno Setup (Профессиональный)

Для создания полноценного установщика (.msi или .exe installer):

1. Сначала создайте .exe через PyInstaller
2. Скачайте Inno Setup: https://jrsoftware.org/isinfo.php
3. Создайте установочный скрипт
4. Соберите installer

---

## ⚠️ Важно для пользователей

### Требования:
- **Windows 10/11**
- **Права администратора** (для первого запуска)

### Первый запуск:
1. Запустите `SharkDraw.exe`
2. Windows может показать предупреждение "Неизвестный издатель"
3. Нажмите "Подробнее" → "Выполнить в любом случае"

### Горячие клавиши:
- `Ctrl+D` - Включить/выключить рисование
- `Ctrl+Shift+C` - Очистить экран
- `ESC` - Выход

---

## 🎯 Рекомендация

Для простой отправки одному человеку - используйте **Вариант 1** (PyInstaller).
Для публичного распространения - используйте **Вариант 3** (Inno Setup).
