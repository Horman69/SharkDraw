# 🔊 Как конвертировать MP3 в WAV

## Проблема
У вас есть звуки в формате MP3, но `winsound` (встроенный модуль Windows) поддерживает **только WAV** формат.

## ✅ Самое простое решение - Онлайн конвертер

1. Откройте: **https://cloudconvert.com/mp3-to-wav**
2. Загрузите ваши 3 файла:
   - `startup.mp3`
   - `click.mp3`
   - `close.mp3`
3. Нажмите "Convert"
4. Скачайте WAV файлы
5. Поместите их в `assets\sounds\` (замените MP3 файлы)

## Альтернатива - Python скрипт

Если хотите конвертировать через Python:

```powershell
# 1. Установите pydub
python -m pip install pydub

# 2. Скачайте ffmpeg
# https://www.gyan.dev/ffmpeg/builds/
# Распакуйте и добавьте в PATH

# 3. Запустите конвертер
python convert_sounds.py
```

## После конвертации

Запустите приложение и проверьте звуки:
```powershell
python main.py
```

Вы должны увидеть в логах:
```
✓ Найден звук: startup (startup.wav)
✓ Найден звук: click (click.wav)
✓ Найден звук: close (close.wav)
```
